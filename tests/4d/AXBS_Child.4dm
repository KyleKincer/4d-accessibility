// Ordinary child initialization. No bridge hooks are required in children.
If (Form event code=On Load)
 Form.first:=Form.tag+" first"
 Form.last:=Form.tag+" last"
 Form.clicks:=0
 Form.wideClicks:=0
 Form.edits:=0
End if
